Cache-status dot — green (hit) / grey (miss). Required on every drug + interaction row.

```jsx
const { CacheDot } = window.DosePairDesignSystem_7db6c7;
<CacheDot hit />
<CacheDot showLabel />  {/* grey dot + "live fetch" */}
```
